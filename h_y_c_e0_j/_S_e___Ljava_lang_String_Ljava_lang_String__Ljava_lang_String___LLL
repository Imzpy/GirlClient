--模板自动生成 请勿修改函数名、参数列表

function eLLL_enter(args)
    local str = getJavaStringContent(args[1])
    print(str)
    local str2 = getJavaStringContent(args[2])
    print(str2)
    return true, args, 0
end

function eLLL_leave(ret)
    local ptext = getJavaStringContent(ret)
    print(ptext)
    return ret
end